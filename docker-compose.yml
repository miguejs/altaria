version: '2'
services:
  db:
    image: postgres
    volumes:
      - 'postgres:/var/lib/postgresql/data'
  altaria:
    build: .
    command: mix phoenix.server
    volumes:
      - .:/app
    ports:
      - "4000:4000"
    depends_on:
      - db